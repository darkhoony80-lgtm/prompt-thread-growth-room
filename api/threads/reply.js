import {decodeSession} from '../../lib-threads-session.js';
const API='https://graph.threads.net/v1.0';
async function call(path,token,method='GET',params=null){let url=`${API}${path}`;const init={method,headers:{Accept:'application/json'}};if(method==='GET'){const u=new URL(url);u.searchParams.set('access_token',token);url=u.toString()}else{const body=new URLSearchParams({...params,access_token:token});init.headers['Content-Type']='application/x-www-form-urlencoded';init.body=body.toString()}const r=await fetch(url,init);const body=await r.json().catch(()=>({}));return {ok:r.ok,status:r.status,body}}
function err(body){const e=body?.error||body||{};return {message:e.message||'unknown',type:e.type||null,code:e.code??null,subcode:e.error_subcode??null}}
function emojiCount(s=''){return (String(s).match(/\p{Extended_Pictographic}/gu)||[]).length}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});const s=decodeSession(req);if(!s?.accessToken||!s?.userId)return res.status(401).json({ok:false,error:'THREADS_NOT_CONNECTED'});const replyToId=String(req.body?.reply_to_id||'').trim();const text=String(req.body?.text||'').trim();if(!replyToId||!text)return res.status(400).json({ok:false,error:'REPLY_AND_TEXT_REQUIRED'});if(text.length>500)return res.status(400).json({ok:false,error:'REPLY_TOO_LONG'});if(emojiCount(text)<2)return res.status(400).json({ok:false,error:'AT_LEAST_TWO_EMOJIS_REQUIRED'});const create=await call('/me/threads',s.accessToken,'POST',{media_type:'TEXT',text,reply_to_id:replyToId});if(!create.ok){const detail=err(create.body);console.log('[THREADS_REPLY_CREATE_FAILED]',JSON.stringify({reply_to_id:replyToId,status:create.status,...detail}));return res.status(502).json({ok:false,error:'THREADS_REPLY_CREATE_FAILED',detail})};const creationId=create.body?.id;if(!creationId)return res.status(502).json({ok:false,error:'THREADS_REPLY_CREATION_ID_MISSING'});const publish=await call('/me/threads_publish',s.accessToken,'POST',{creation_id:String(creationId)});if(!publish.ok){const detail=err(publish.body);console.log('[THREADS_REPLY_PUBLISH_FAILED]',JSON.stringify({reply_to_id:replyToId,creation_id:String(creationId),status:publish.status,...detail}));return res.status(502).json({ok:false,error:'THREADS_REPLY_PUBLISH_FAILED',detail})};console.log('[THREADS_REPLY_PUBLISHED]',JSON.stringify({reply_to_id:replyToId,published_id:publish.body?.id||null}));return res.status(200).json({ok:true,id:publish.body?.id||null})}
